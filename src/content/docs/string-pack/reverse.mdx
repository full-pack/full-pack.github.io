---
title: Reverse
description: Reverses the character sequence of the string in javascript/typescript.
head:
    - tag: title
      content: String-Pack | Reverse
tableOfContents: false
---

import { Code } from '@astrojs/starlight/components'

Reverses the sequence of characters in given string.

#### Parameters: `string` 

- A string.
 
#### Returns: `string`

- The string in reverse order.

<Code
    frame='code'
    title="Definition"
    lang="ts"
    code="function reverse(str: string): string"
/>

export const reverse = `reverse('bad') // 'dab'`

<Code frame='code' title='Example' code={reverse} lang="js" />
