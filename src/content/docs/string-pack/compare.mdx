---
title: Compare
description: Compare two strings using compare for strict equality check and looseCompare for case-insensitive equality check in javascript/typescript.
head:
    - tag: title
      content: String-Pack | Compare
---

import { Code } from '@astrojs/starlight/components'

Checks equality of two strings.

### compare

Performs a case-sensitive strict comparison between two strings.

#### Parameters

- `str1`: `string` - The first string for comparison.
- `str2`: `string` - The first string for comparison.
 
#### Returns: `boolean`

- Returns true if the strings are equal; otherwise, false.

<Code
    frame='code'
    title="Definition"
    lang="ts"
    code="function compare(str1: string, str2: string): boolean"
/>

export const compare =
  `compare('hello', 'hello') // true

compare('abc', 'ABC') // false`;

<Code frame='code' title='Examples' code={compare} lang="js" />

### looseCompare

Performs a case-insensitive loose comparison between two strings.

#### Parameters

- `str1`: `string` - The first string for comparison.
- `str2`: `string` - The first string for comparison.
 
#### Returns: `boolean`

- Returns true if the strings are equal (case-insensitive); otherwise, false.

<Code
    frame='code'
    title="Definition"
    lang="ts"
    code="function looseCompare(str1: string, str2: string): boolean"
/>

export const looseCompare =
  `looseCompare('CAps', 'caPS') // true

looseCompare('abc', '123') // false`;

<Code frame='code' title='Examples' code={looseCompare} lang="js" />
